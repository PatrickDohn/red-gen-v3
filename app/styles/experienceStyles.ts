export const experienceStyle: Record<string, React.CSSProperties> = {
  experienceSection: {
    width: "60%",
    margin: 10,
    marginTop: 0,
    padding: 10,
    paddingTop: 0,
    paddingBottom: 0,
    marginBottom: 10,

    // border: "2px solid red",
  },
  heading: {
    display: "flex",
    flexDirection: "row",
    borderBottom: "2px solid",
    borderColor: "#B0BEC5",
    marginBottom: 5,
  },
  iconText: {
    fontSize: 15, // Sets the icon size
    // marginBottom: 4,
    paddingTop: 2.8, // Spaces the icon from the text
    marginRight: 5,

    width: 15, // Give it an explicit width for clean alignment
    color: "#004F7A",
  },
  title: {
    fontSize: 16.1,
    fontFamily: "JetBrains Mono",
    fontWeight: 800,
    lineHeight: 1.5,

    textAlign: "center",
  },
  experienceContainer: {
    marginBottom: 3,
  },

  positionTitleRow: {
    marginBottom: 2,
  },
  positionTitleText: {
    fontSize: 10,
    fontWeight: 700,
    color: "#212121",
  },
  positionCompanyAccent: {
    fontSize: 10,
    fontWeight: 700,
    color: "#004F7A", // Your accent color
  },

  text: {
    fontSize: 10,
    // marginBottom: 2,
  },
  dutyRow: {
    display: "flex",
    flexDirection: "row", // CRITICAL: Makes bullet and text go side-by-side
    // marginBottom: 3,
    marginLeft: 15,
  },
  dutyText: {
    display: "flex",
    flex: 1, // CRITICAL: Takes remaining space, forcing indent
    fontSize: 10,
  },
  bullet: {
    fontSize: 6, // Sets the icon size
    marginTop: 5,
    marginRight: 4,
    width: 6,
    color: "#004F7A",
  },
};


export const experienceStyleTwo: Record<string, React.CSSProperties> = {
  experienceSection: {
    width: "100%",
    margin: 10,
    marginTop: 0,
    padding: 10,
    paddingTop: 0,
    paddingBottom: 0,
    marginBottom: 10,

    // border: "2px solid red",
  },
  heading: {
    display: "flex",
    flexDirection: "row",
    borderBottom: "2px solid",
    borderColor: "#B0BEC5",
    marginBottom: 5,
  },
  iconText: {
    fontSize: 15, // Sets the icon size
    // marginBottom: 4,
    paddingTop: 2.8, // Spaces the icon from the text
    marginRight: 5,

    width: 15, // Give it an explicit width for clean alignment
    color: "#004F7A",
  },
  title: {
    fontSize: 16.1,
    fontFamily: "JetBrains Mono",
    fontWeight: 800,
    lineHeight: 1.5,
    color: "#212121",
    textAlign: "center",
  },
  experienceContainer: {
    marginBottom: 3,
  },

  positionTitleRow: {
    marginBottom: 2,
  },
  positionTitleText: {
    fontSize: 10,
    fontWeight: 700,
    color: "#212121",
  },
  positionCompanyAccent: {
    fontSize: 10,
    fontWeight: 700,
    color: "#004F7A", // Your accent color
  },

  text: {
    fontSize: 10,
    // marginBottom: 2,
  },
  dutyRow: {
    display: "flex",
    flexDirection: "row", // CRITICAL: Makes bullet and text go side-by-side
    // marginBottom: 3,
    marginLeft: 15,
  },
  dutyText: {
    display: "flex",
    flex: 1, // CRITICAL: Takes remaining space, forcing indent
    fontSize: 10,
  },
  bullet: {
    fontSize: 6, // Sets the icon size
    marginTop: 5,
    marginRight: 4,
    width: 6,
    color: "#004F7A",
  },
};
